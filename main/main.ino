#include "./defines.h"
#include <ESP8266WiFi.h>

void setup()
{
    pinMode(LED, OUTPUT);
    digitalWrite(LED, HIGH);    // Note: on ESP-12E, pin needs to be HIGH to disable built-in LED
    // code...
}

void loop()
{
    // code...
}